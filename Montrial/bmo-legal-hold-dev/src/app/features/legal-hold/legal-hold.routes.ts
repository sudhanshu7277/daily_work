import { Routes } from '@angular/router';
import { LegalHoldShellComponent } from './legal-hold-shell.component';

export const LEGAL_HOLD_ROUTES: Routes = [
  { path: '', component: LegalHoldShellComponent }
];