export interface LegalHoldRecord {
  legalName: string; 
      ocifId: string;
      status: string;
      holdName: string;
      lifecycle: string;
      role: string;
      address: string;
  // legalName: string;
  // ocifId: string;
  // status: string;
  // holdName: string;
  // lifecycle: string;
  // role: string;
  // address: string;
}